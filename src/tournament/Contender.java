package tournament;

public class Contender extends Fighter {

    public Contender(String name, int health, int power, int blockValue, String quote) {
        super(name, health, power, blockValue, quote);
    }
}
