package tournament;

class Contender extends Fighter {

    Contender(String name, int health, int power, int blockValue, String quote) {
        super(name, health, power, blockValue, quote);
    }
}
